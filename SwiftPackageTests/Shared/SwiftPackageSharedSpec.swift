@testable import SwiftPackage
import Quick
import Nimble

class SwiftPackageSharedSpec: QuickSpec {

  override func spec() {
    describe("SwiftPackageShared") {
      describe("test") {
        it("passes") {
          expect(true).to(beTrue())
        }
      }
    }
  }
}
